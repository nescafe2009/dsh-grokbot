# DESIGN v3 — dsh-grokbot（依据 issue #3 Codex v3 方案，2026-09-06）

> v2 归档于 git 历史。v3 依据用户决策：**界面像 Grok，运行回归原生 DSH 插件，默认宿主 macOS，可分享**。

## 产品定义（三优先级同时成立）

1. **界面有明确参照**：Grok 的布局/留白/消息与卡片观感（六状态冻结，见 R1）
2. **工作高效可靠**：普通问题直接答；需要工具直接用 DSH；确有分工才交接，结果回原会话
3. **安装容易分享**：一个插件包 + 兼容 DSH + 用户自己的模型；不要求部署服务器/复制运行时

范围：本轮 macOS 实测宿主版本；退出/睡眠不承诺继续执行，重开后准确恢复。完整契约见 issue #3 v3 评论。

## 架构：一个插件，复用一个宿主

```
Grok 风格 UI（私聊/群聊/任务卡/文件卡）
   ↕ 插件（身份/会话归属/定向交接/任务摘要）
   ↕ DSH（模型/agent/session/工具/权限/事件持久化）
   → 用户本机工作区
```

- 执行工具用宿主原生（bash/fs——经 cordis.patch.yml 启用 web profile 默认禁用项）
- VM/SSH/noVNC = 可选配件（computer.json 驱动，默认不启用），不在安装链
- 存储：插件自管轻量（追加日志+原子快照），不引入 node:sqlite 等版本门槛
- UI 挂载：宿主 slots 为主，CSS 接管收敛为单一版本适配层（R1），停用完整清理

## 数据模型（逻辑记录，非七张表）

Bot / Conversation（稳定 kind，不按人数变型）/ Message（请求去重标识）/
SessionBinding（conv+bot 或 task+bot → DSH session）/ Task·Run（持续工作·一次执行）/
Artifact（来源 run + size/SHA/MIME 固定快照）/ Delivery（待回传+稳定事件键）。
DSH 事件流是执行细节权威；插件只存身份映射、用户可见消息、任务/交付状态。

## 批次与门槛

| 批 | 内容 | 门槛 |
|---|------|------|
| R0 底座 | 无损迁回/能力映射/默认本机工作区/去 VM 必需链 | 可聊可执行、数据核对、单一消费实例 |
| R1 界面 | Grok 六状态冻结（空态/私聊/群聊/执行中/交付/窄窗）、token+五组件、本机工具与文件交付 | 界面按参照通过；聊天→成果完整可用 |
| R2 接力 | 来源归属/精确@/Task·Run/继续·取消·恢复 | 交错不串、接力同版本、停止真实、重开不重派 |
| R3 分享 | 实际包/兼容文档/去个人环境依赖 | 干净 profile 安装到交付通过 |

通用缺陷（唤醒丢失/队列伪取消等）在所属流程验收前修复。完整验收集见 issue #3 v3 §10。
